{
  "name": "flatadango-api",
  "version": "1.0.0",
  "description": "A simple JSON server for Flatdango app",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 4000 --host 0.0.0.0 --cors"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
